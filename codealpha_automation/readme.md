# 🌐 Web Page Title Extractor (Python)
A simple Python script that fetches a live static webpage and extracts its HTML `<title>` using **BeautifulSoup** with the **lxml parser**.
This project demonstrates basic web scraping concepts using beginner-friendly code.

## 🎯 Project Goal
- Access a live static webpage
- Parse the HTML content
- Extract and display the page title
- Use minimal and simple Python code

## 🛠️ Technologies Used
- Python 3
- `requests`
- `beautifulsoup4`
- `lxml`